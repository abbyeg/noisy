// use perlin::Perlin;

fn main() {
  // let pn = Perlin::new(42);
  // let noise_img =  perlin::generate_perlin_image(&pn, 384, 256, 2.0);
  // noise_img.save("perlin_noise.png").expect("Failed to save image");
  // println!("perlin noise: {}",);
}
